# TODO

- [x] **Agent D: Add web fallback for opinion lookups.** Agent D now launches whenever ND citations are found (no longer requires `~/refs/opin/`). Fallback order: (1) ndcourts.gov citation search (`?cit1=YYYY&citType=ND&cit2=NNN`) returns highlight/syllabus text; (2) CourtListener search API returns syllabus + snippet. Note: the simple `?search=` parameter on ndcourts.gov is JS-rendered and doesn't work with WebFetch — the structured citation parameters are required.
- [x] **Agent D: Expand to non-ND case citations.** jetcite now generates URLs (CourtListener, Justia, etc.) for federal reporters, U.S. Supreme Court, state reporters, and non-ND neutral citations. Agent D's launch condition and prompt expanded to handle all case citation types using jetcite-provided URLs. No separate cite2url table needed — jetcite's `Citation.sources` serves that role.
- [x] **Smarter PDF text extraction.** `extract_text.py` tries multiple extractors (pdftotext → pypdf → PyMuPDF → pdfplumber → marker) in priority order with early exit on good quality. Falls back to visual read when all fail.
- [x] **jetcite integration.** `verify_citations.py` rewritten as a thin jetcite wrapper. Agents D and E receive structured JSON with local paths, URLs, and search hints instead of constructing them manually.
- [x] **Docx output.** `memo_to_docx.py` converts markdown memos to formatted .docx matching the Court's bench memo template (QTPalatine 13pt, styled headings, page numbers).
